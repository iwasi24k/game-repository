name: AI PR Review

on:
  pull_request:
    branches:
      - develop
    types: [opened, synchronize, ready_for_review]

jobs:
  review:
    if: >
      github.event.pull_request.draft == false &&
      !contains(github.event.pull_request.title, '[skip ai]')

    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - name: Get diff
        run: |
          git fetch origin develop
          git diff origin/develop > diff.txt

      - name: Comment PR with AI Review
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DIFF=$(cat diff.txt)

          if [ -z "$DIFF" ]; then
            echo "No diff found. Skip AI review."
            exit 0
          fi

          REVIEW=$(curl -s https://api.openai.com/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -d "{
              \"model\": \"gpt-4.1-mini\",
              \"messages\": [
                {
                  \"role\": \"system\",
                  \"content\": \"あなたはシニアソフトウェアエンジニアです。以下のプルリクエストの差分コードをレビューし、問題点・改善点・潜在的なバグを簡潔かつ建設的に指摘してください。\"
                },
                {
                  \"role\": \"user\",
                  \"content\": \"$DIFF\"
                }
              ]
            }" | jq -r '.choices[0].message.content')

          gh pr comment ${{ github.event.pull_request.number }} \
            --body "### 🤖 AI Code Review\n\n$REVIEW"
